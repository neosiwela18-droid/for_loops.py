# for_loops.py
The code creates two lists of names with mixed capitalization, then prompts the user to input two additional names. It loops through the user-entered names first, printing a birthday invitation to each one with proper capitalization using .title(). ......
